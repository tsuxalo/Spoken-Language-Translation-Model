# CUDA environment for GPU experiments.
#
# Use this only on a machine whose NVIDIA driver supports this CUDA build.
# University clusters may require a different PyTorch CUDA wheel.

-r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.13.0+cu130