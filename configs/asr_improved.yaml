kind: asr
dataset:
  id: google/fleurs
  revision: 70bb2e84b976b7e960aa89f1c648e09c59f894dd
  config_name: ha_ng
  train_split: train
  validation_split: validation
  test_split: test
  target_column: raw_transcription
  sampling_rate: 16000
  max_duration_seconds: 30.0
model:
  id: nahomazmach/whisper-small-ha
  revision: c4e2b47d88ae8b3ee0a605e09863b93aafca72e3
  language: Hausa
  task: transcribe
  efficiency_strategy: full
training:
  output_dir: artifacts/checkpoints/whisper-small-ha-asr-v2
  seed: 42
  learning_rate: 0.000005
  train_batch_size: 2
  eval_batch_size: 2
  gradient_accumulation_steps: 4
  num_train_epochs: 5
  warmup_ratio: 0.05
  max_grad_norm: 1.0
  gradient_checkpointing: true
  eval_strategy: epoch
  save_strategy: epoch
  save_total_limit: 2
  early_stopping_patience: 2
  precision: auto
tracking:
  run_name: whisper-small-ha-asr-v2
